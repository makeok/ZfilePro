<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="im.zhaojun.zfile.module.login.mapper.UsersMapper">
    <resultMap id="BaseResultMap" type="im.zhaojun.zfile.module.login.model.entity.User">
        <!--@mbg.generated-->
        <!--@Table users-->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="username_zh" jdbcType="VARCHAR" property="usernameZh"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="enabled" jdbcType="BIT" property="enabled"/>
        <result column="role" jdbcType="VARCHAR" property="role"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id, `username`,username_zh, enabled, role, remark
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from users
        where 1=1 order by username
    </select>

    <select id="selectEnabled" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from users
        where `enabled`=#{enabled,jdbcType=BIT} order by username
    </select>

    <select id="findOneById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from users
        where `id`=#{id,jdbcType=INTEGER} order by username
    </select>

    <update id="updateFieldsById">
        update users set username = #{username},
            username_zh = #{usernameZh},
            enabled = #{enabled},
            role = #{role},
            remark = #{remark}
        where id = #{id}
    </update>
    <update id="updateZhById">
        update users set
        username_zh = #{usernameZh},
        remark = #{remark}
        where id = #{id}
    </update>

    <update id="updatePasswdById">
        update users set password = #{password}
        where id = #{id}
    </update>

    <update id="updateEnabledById">
        update users set enabled = #{enabled}
        where id = #{id}
    </update>

<!--auto generated by MybatisCodeHelper on 2021-07-15-->
    <insert id="insertList" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users(
            username,
            username_zh,
            password,
            enabled,
            role,
            remark
        )VALUES
        <foreach collection="list" item="element" index="index" separator=",">
            (
                #{element.name,jdbcType=VARCHAR},
                #{element.usernameZh,jdbcType=VARCHAR},
                #{element.password,jdbcType=VARCHAR},
                #{element.enabled,jdbcType=BIT},
                #{element.role,jdbcType=VARCHAR},
                #{element.remark,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>
</mapper>